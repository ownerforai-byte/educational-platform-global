#!/bin/bash
# Stage all changes
git add -A

# Commit with descriptive message
git commit -m "feat: add all educational platform features - 3D viewer, quizzes, walkthroughs, collaboration, analytics, and more"

# Push to remote
git push
