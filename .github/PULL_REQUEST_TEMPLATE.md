<!-- Read BRANCHES.md before filling this out. -->

## Area branch

- **Branch:** `<!-- e.g. content/physics -->`
- **Area (per BRANCHES.md):** `<!-- e.g. content/ravikishan/class-11-notes/physics/** -->`
- [ ] This PR only touches files inside its branch's area (or the description below explains why not)

## Checks

- [ ] `npm run check` passes (scoped to what I changed)
- [ ] `npm run check:all` passes (required: this targets `main`)
- [ ] No secrets in the diff (`git grep -n -E "vcp_|sbp_|AIza|sk-|KEY=|TOKEN="`)

## What & why

<!-- Short description. Use task labels from project-conductor.md §3:
     feature: / bugfix: / scrape: / transform: / load: / setup: / config: / docs: / test: -->
